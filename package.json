{
  "name": "root-plotting-tool",
  "private": true,
  "description": "ROOT-A-TRON 3000: Cloudflare deployment (pages as static assets, fitting API as a container).",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7"
  },
  "devDependencies": {
    "wrangler": "^4.135.0"
  }
}
